info:
  name: Hello me!
  type: http
  seq: 1

http:
  method: GET
  url: "{{url}}/hello/me"
  headers:
    - name: Authorization
      value: "{{access_token}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
