info:
  name: View a company
  type: http
  seq: 2

http:
  method: GET
  url: "{{url}}/company/{{id}}"
  headers:
    - name: Authorization
      value: "{{access_token}}"
  auth: inherit

runtime:
  variables:
    - name: id
      value: "3"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
