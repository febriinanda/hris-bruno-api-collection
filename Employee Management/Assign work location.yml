info:
  name: Assign work location
  type: http
  seq: 5

http:
  method: POST
  url: "{{url}}/employee/{{id}}/work/site"
  headers:
    - name: Authorization
      value: "{{access_token}}"
  body:
    type: json
    data: |-
      {
        "office":{
          "id":4
        },
        "startDate":"{{$randomDateFuture}}",
        "endDate":null
      }
  auth: inherit

runtime:
  variables:
    - name: id
      value: "3"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
